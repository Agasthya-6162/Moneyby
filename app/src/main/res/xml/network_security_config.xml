<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Force HTTPS throughout the app -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Placeholder for specific domains and SSL Pinning -->
    <!-- 
    <domain-config>
        <domain includeSubdomains="true">api.moneyby.com</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">BASE64_CERT_HASH_HERE</pin>
            <pin digest="SHA-256">BACKUP_CERT_HASH_HERE</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>
